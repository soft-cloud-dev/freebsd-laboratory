# Applied when if_bridge is loaded through rc.subr(8).
# Filter on labbridge0 itself and avoid applying the same PF rules to every
# epair/tap bridge member.

net.link.bridge.pfil_bridge=1
net.link.bridge.pfil_member=0
