# Filter packets on the bridge interface itself. The laboratory PF anchor is
# written against labbridge0, so bridge filtering must be enabled there.
net.link.bridge.pfil_bridge=1

# Avoid duplicate filtering on every epair/tap member. Runtime-to-host policy is
# enforced at labbridge0 instead.
net.link.bridge.pfil_member=0
